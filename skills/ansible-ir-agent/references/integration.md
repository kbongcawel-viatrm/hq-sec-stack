# Ansible IR Integration Reference

Ansible is the action executor. Preferred flow: TheHive task approval -> Shuffle or analyst runs playbook in `ansible-ir` -> output and changed state recorded back to the case.
